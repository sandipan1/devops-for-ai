apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Values.web.configMapName }}
  labels:
    app: {{ .Values.web.name }}
    {{- include "synchat.labels" . | nindent 4 }}
data:
  WEB_PORT: {{ .Values.web.config.webPort | quote }}
  API_URL: {{ .Values.web.config.apiUrl | quote }}
