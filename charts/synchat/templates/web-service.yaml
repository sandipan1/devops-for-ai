apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.web.serviceName }}
  labels:
    app: {{ .Values.web.name }}
    {{- include "synchat.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  selector:
    app: {{ .Values.web.name }}
  ports:
    - name: http
      protocol: TCP
      port: {{ .Values.web.servicePort }}
      targetPort: http
